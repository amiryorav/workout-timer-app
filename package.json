{
  "name": "workout-timer-app",
  "version": "1.0.0",
  "description": "Browser-based interval timer for workouts",
  "private": true,
  "scripts": {
    "start": "parcel index.html editor.html",
    "build": "parcel build index.html editor.html",
    "clean": "rimraf dist .parcel-cache",
    "prebuild": "npm run clean",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.14.4",
    "@parcel/transformer-webmanifest": "^2.14.4",
    "gh-pages": "^6.3.0",
    "parcel": "^2.8.3",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Safari versions",
    "last 2 Edge versions"
  ]
}
